
It would be nice to have good support for two naming conventions for ticket files
    - by default use double digit numbers, in sequence, as a prefix
    - if available, use an identifier from an external system as a prefix

I'd like to adjust the framework to use the ticket file name in the docs/plans and docs/implementations folders.
    - the problem is I would also like to move completed tickets and their related files to completed, and that would cause a name collision
    - we could combine them?  or create a new summary?
    - we could add folder structure under docs/completed to match?
    - we could add an action after the ticket identifier?

